server:
  port: 7777
eureka:
  hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.hostname}:${server.port}/eureka/